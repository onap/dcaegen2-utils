# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py27,py35

[testenv]
deps=
    pytest
    coverage
    pytest-cov
commands=
    #pytest --junitxml xunit-results.xml --cov {envsitepackagesdir}/dockering --cov-report=xml
    pytest --verbose --cov dockering --cov-report=xml --cov-report=term tests
